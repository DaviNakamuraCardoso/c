#include <stdio.h>
#include "filters.h"

void blur(char *filename)
{
    printf("Applying blur to %s\n", filename);
}

void contrast(char *filename)
{
    printf("Applying contrast to %s\n", filename);
}

void gray(char *filename)
{
    printf("Applying grayscale to %s\n", filename);
}

void reflex(char *filename)
{
    printf("Applying reflex to %s\n", filename);
}
